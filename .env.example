# Sierra — environment template
# Copy to .env and fill all values. NEVER commit .env.
# Generate secrets: python -c "import secrets; print(secrets.token_hex(32))"

# ── Application ────────────────────────────────────────────────────────────────
SIERRA_ENV=development

# ── Security ───────────────────────────────────────────────────────────────────
SESSION_SECRET=<generate-strong-random-64-hex-chars>
ARGON2_PEPPER=<generate-strong-random-64-hex-chars>
ALLOWED_ORIGINS=https://localhost

# ── MQTT ───────────────────────────────────────────────────────────────────────
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_USER=sierra-backend
MQTT_PASS=<generate-strong-random>
SIERRA_HUB_MQTT_USER=sierra-hub
SIERRA_HUB_MQTT_PASS=<generate-strong-random>

# ── App settings ───────────────────────────────────────────────────────────────
TIMEZONE=Europe/Rome
LOG_LEVEL=INFO

# ── Network binding ────────────────────────────────────────────────────────────
# Default 0.0.0.0: UI and broker are reachable on the LAN (the hub use case —
# phones/tablets open https://<hub-ip>/, ESP devices connect to the broker).
# On a laptop without hardware where you don't want LAN exposure, set 127.0.0.1.
HTTP_BIND=0.0.0.0
MQTT_BIND=0.0.0.0
BACKEND_BIND=127.0.0.1
